_subdirectory: template

_template_version:
    type: str
    default: >-
        [[ run("grep -m 1 '^[[:space:]]*version[[:space:]]*=' '" ~ _copier_conf.src_path ~ "/pyproject.toml' | tr -d ' \"' | cut -d'=' -f2") ]]

project_name_kebab_case:
    type: str
    help: project_name_kebab_case (this-is-kebab-case)
